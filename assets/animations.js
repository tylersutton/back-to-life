Game.Arrow = function(properties) {
    properties = properties || {};
    Game.Animation.call(this, properties);
};
Game.Arrow.extend(Game.Animation);

Game.ScrollTrail = function(properties) {
    properties = properties || {};
    Game.Animation.call(this, properties);
};
Game.ScrollTrail.extend(Game.Animation);


